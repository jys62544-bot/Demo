# Empty uses same-origin /api. In local Vite dev, vite.config.ts proxies /api and /uploads to the local Worker API at 127.0.0.1:8787.
VITE_API_BASE_URL=
VITE_USE_MOCK=false
VITE_AGENT_TIMEOUT_MS=70000
