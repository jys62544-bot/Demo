# 工业现场多模态数据采集与智能管理平台 — 详细设计计划

> 版本：v1.1
> 日期：2026-05-25
> 基于：《工业现场多模态数据采集与智能管理平台 Demo 设计文档 v1.1》
> 目标：将设计文档转化为可直接指导两名开发者/Agent 并行开发的结构化实施计划

---

## 0. 审查结论与修正原则

### 0.1 当前计划存在的问题

1. **实现路线过多，容易失控。** 原计划同时展开 FastAPI、本地服务器、Cloudflare Workers、Cloudflare Pages、GitHub Pages、Nginx/systemd，多条路线混在同一个开发阶段里，会让 Demo 首版开发被部署选型拖慢。
2. **并行边界不清。** 后端、前端、部署、README、`.gitignore` 等共享文件没有明确归属，两名 Agent 同时开发时容易改同一批文件。
3. **API 契约没有作为并行开发前置锁定项。** 前端必须能在后端未完成时基于 mock/fixture 先做页面，后端必须按固定响应结构实现接口，否则集成阶段会返工。
4. **验收标准偏总括，缺少两条开发线各自的阶段验收。** 原阶段划分是串行的，不利于两个人同时推进。
5. **部署目标和 Demo 目标混杂。** 当前目标是 5 分钟可演示 Demo，生产部署、R2、D1、Nginx/systemd 应作为延期项或可选项，而不是首轮必做项。

### 0.2 修正后的执行原则

1. **首版 MVP 只做一条主线：** `backend/FastAPI + SQLite + 本地 uploads/` 和 `frontend/React + Vite + TypeScript`。
2. **Cloudflare Workers、D1、R2、GitHub Pages、Nginx/systemd 全部降级为二期或部署备选，不阻塞首版 Demo。**
3. **两名 Agent 按目录隔离并行开发：**
   - Agent A：负责 `backend/`、后端 API、数据库、seed、Agent mock/proxy。
   - Agent B：负责 `frontend/`、页面、路由、主题、图表、前端 mock 与 API 对接。
4. **共享契约先冻结：** API 路径、字段名、枚举、响应结构以第 4-6 节为准；如需变更，必须先同步修改本文档。
5. **共享根文件延后统一处理：** `README.md`、根 `.gitignore`、部署文件由集成阶段统一补齐，避免两名 Agent 并行冲突。
6. **每名 Agent 都必须先读全局计划，再只执行自己的分工说明。** 本文末尾第 15 节提供可直接复制给两名 Agent 的任务说明。

---

## 目录

1. [项目概述](#1-项目概述)
2. [技术架构设计](#2-技术架构设计)
3. [系统角色与页面规划](#3-系统角色与页面规划)
4. [数据库设计](#4-数据库设计)
5. [API 接口设计](#5-api-接口设计)
6. [核心业务逻辑](#6-核心业务逻辑)
7. [前端设计规范](#7-前端设计规范)
8. [后端设计规范](#8-后端设计规范)
9. [Demo 数据设计](#9-demo-数据设计)
10. [部署方案](#10-部署方案)
11. [开发阶段划分与任务清单](#11-开发阶段划分与任务清单)
12. [演示脚本](#12-演示脚本)
13. [验收清单](#13-验收清单)
14. [后续扩展方向](#14-后续扩展方向)
15. [双 Agent 并行开发分工](#15-双-agent-并行开发分工)

---

## 1. 项目概述

### 1.1 一句话定位

面向工业现场的多模态数据采集与智能管理平台 Demo —— 支持视频/图片/音频/文档/文本上传、知识资产自动沉淀、员工贡献激励和 AI Agent 辅助决策的可视化平台原型。

### 1.2 核心 Demo 价值

| 能力维度 | 说明 |
|---------|------|
| 多模态数据入口 | 工业现场视频、图片、音频、文档、文本统一进入系统 |
| 知识沉淀闭环 | 上传数据自动转化为知识条目 |
| 管理可视化 | 数据增长、异常案例、知识图谱、贡献排行一屏总览 |
| Agent 扩展能力 | AI 能力通过 API 接入，mock/proxy 双模式，可随时替换为真实模型 |

### 1.3 版本边界

**当前版本实现：**

1. 员工端登录 + 多模态文件上传 + 元数据填写
2. 上传后自动生成知识条目 + 贡献积分 + 异常案例
3. 管理端大屏（统计卡片、实时数据流、贡献排行榜、异常案例、数据趋势图）
4. 管理端知识库列表 + 模拟知识图谱（ECharts Graph）
5. Agent 模块：mock 模板答案 + proxy 外部 API 转发
6. 完整 Demo 演示闭环（5 分钟路演）

**当前版本不做：**

- 真实 RAG / 向量数据库
- 模型训练（视觉/动作识别/异常检测）
- 真实知识图谱数据库（Neo4j）
- 复杂权限/审批/安全审计/高并发/生产级对象存储

---

## 2. 技术架构设计

### 2.1 总体架构图

```text
┌───────────────────────────────────────────────────────┐
│                      前端系统                          │
│                  React + Vite + TypeScript              │
│                                                        │
│  员工端（马卡龙配色）         管理端（蓝紫科技风）         │
│  ├─ 登录页                  ├─ 管理驾驶舱               │
│  ├─ 员工首页                ├─ 实时数据流               │
│  ├─ 多模态上传              ├─ 知识库展示               │
│  ├─ AI 助手                 ├─ 知识图谱                 │
│  └─ 我的贡献                ├─ 异常案例                 │
│                             ├─ 贡献排行榜               │
│                             └─ Agent 决策问答           │
└──────────────────────▲────────────────────────────────┘
                       │ HTTP API
┌──────────────────────┴────────────────────────────────┐
│                    后端服务                             │
│          首版 MVP: FastAPI + SQLite + 本地 uploads        │
│          二期备选: Cloudflare Workers + D1 + R2           │
│                                                        │
│  登录认证 / 文件上传 / 元数据管理 / 统计接口              │
│  知识条目生成 / 积分生成 / Agent API 转发                │
└──────────────────────▲────────────────────────────────┘
                       │
       ┌───────────────┼───────────────┐
       │               │               │
┌──────┴──────┐ ┌──────┴──────┐ ┌──────┴──────────┐
│  数据库      │ │  文件存储    │ │  外部 Agent API  │
│ SQLite/D1   │ │ uploads/R2  │ │ OpenAI-Compatible│
└─────────────┘ └─────────────┘ └─────────────────┘
```

### 2.2 首版 MVP 技术路线

| 层级 | 首版选型 | 原因 |
|------|----------|------|
| 前端 | React 18 + Vite + TypeScript | 开发快、生态成熟、适合 Demo 页面和图表 |
| 后端 | FastAPI + Uvicorn | 本地启动简单，接口文档自动生成，便于两端联调 |
| 数据库 | SQLite + SQLAlchemy | 单文件数据库足够支撑 Demo，初始化和迁移成本低 |
| 文件存储 | 本地 `backend/uploads/` | 不依赖云账号，不阻塞演示 |
| Agent | `mock` 默认，`proxy` 可选 | 默认稳定演示，有 Key 时可切真实模型 |
| 部署 | 本地双服务启动 | 优先保证 5 分钟演示闭环 |

### 2.3 二期/备选路线

| 路线 | 状态 | 启用条件 |
|------|------|----------|
| Cloudflare Workers + Hono | 二期备选 | 需要公网无服务器 API，且首版本地 Demo 已验收 |
| Cloudflare D1 + R2 | 二期备选 | 需要云端数据库和对象存储 |
| Cloudflare Pages | 二期备选 | 前端需要公网访问 |
| GitHub Pages 静态演示 | 应急备选 | 后端无法部署，只展示纯前端 Demo |
| Nginx + systemd | 服务器部署备选 | 已获得云服务器并需要长期运行 |

### 2.4 前端技术栈

| 技术 | 用途 |
|------|------|
| React 18 | UI 框架 |
| Vite | 构建工具 |
| TypeScript | 类型安全 |
| React Router | 路由管理 |
| Ant Design | UI 组件库 |
| ECharts | 图表 + 知识图谱 |
| Axios | HTTP 请求 |
| dayjs | 日期处理 |
| Zustand 或 React Context | 状态管理 |

### 2.5 后端技术栈（首版 MVP — FastAPI）

| 技术 | 用途 |
|------|------|
| Python 3.10+ | 运行时 |
| FastAPI | Web 框架 |
| Uvicorn | ASGI 服务器 |
| SQLAlchemy | ORM |
| SQLite | 数据库 |
| python-multipart | 文件上传解析 |
| pydantic | 数据校验 |
| httpx | 异步 HTTP 客户端（调用外部 Agent API） |

### 2.6 后端技术栈（二期备选 — Cloudflare Workers）

| 技术 | 用途 |
|------|------|
| TypeScript | 开发语言 |
| Hono | 轻量 Web 框架 |
| Cloudflare D1 | 数据库 (SQLite 兼容) |
| Cloudflare R2 | 对象存储 |
| Wrangler | 开发/部署 CLI |

---

## 3. 系统角色与页面规划

### 3.1 角色定义

| 角色 | 端 | 核心职责 |
|------|------|----------|
| 员工 (employee) | 员工端 | 上传多模态数据、使用 AI 助手、查看贡献 |
| 管理员 (admin) | 管理端 | 查看大屏、数据分析、知识图谱、决策问答 |

### 3.2 预置账号

```text
员工端：employee / 123456  → 跳转 /employee/dashboard
管理端：admin / 123456     → 跳转 /admin/dashboard
附加员工：worker2 / 123456, worker3 / 123456
```

### 3.3 完整路由表

| 路径 | 页面 | 端 | 说明 |
|------|------|------|------|
| `/login` | 登录页 | 公共 | 统一登录入口 |
| `/employee/dashboard` | 员工首页 | 员工端 | 指标卡片+快捷入口+最近记录 |
| `/employee/upload` | 多模态上传 | 员工端 | 核心上传页 |
| `/employee/assistant` | AI 助手 | 员工端 | 三类 Agent 问答 |
| `/employee/contribution` | 我的贡献 | 员工端 | 积分+贡献历史 |
| `/admin/dashboard` | 管理驾驶舱 | 管理端 | 核心大屏展示 |
| `/admin/knowledge` | 知识库 | 管理端 | 知识条目列表 |
| `/admin/graph` | 知识图谱 | 管理端 | ECharts 力导向图 |
| `/admin/abnormal` | 异常案例 | 管理端 | 风险等级+案例列表 |
| `/admin/ranking` | 贡献排行榜 | 管理端 | 员工积分排行 |
| `/admin/decision-agent` | Agent 决策 | 管理端 | 管理决策问答 |

### 3.4 员工端功能矩阵

| 模块 | 功能点 |
|------|--------|
| 员工首页 | 欢迎区、4 项指标卡片（今日上传/知识贡献/积分/AI问答次数）、4 个快捷入口、最近 5 条上传 |
| 多模态上传 | 拖拽上传区、元数据表单（10 字段）、上传进度、成功提示+积分反馈、最近上传列表 |
| AI 助手 | 3 类 Agent 卡片选择、聊天窗口、示例问题快捷按钮 |
| 我的贡献 | 积分卡片、积分明细表、上传记录列表、知识条目列表、贡献徽章 |

### 3.5 管理端功能矩阵

| 模块 | 功能点 |
|------|--------|
| 管理驾驶舱 | 6 项核心指标卡片、实时上传数据流（最近10条）、贡献排行TOP5、异常案例列表、7天趋势图 |
| 知识库 | 筛选区（关键词/类型/设备/工序/状态/贡献人/异常）、知识条目表格、查看详情 |
| 知识图谱 | ECharts Graph 力导向图、6 类节点 + 5 类边、筛选、悬停/点击交互 |
| 异常案例 | 风险统计卡片（按等级）、异常案例表格、详情弹窗+AI建议 |
| 贡献排行榜 | 总榜（排名/姓名/部门/上传数/知识数/异常数/总积分） |
| Agent 决策 | 2 类决策 Agent、问题输入、结构化结果卡片（结论/依据/风险/建议/优先级） |

---

## 4. 数据库设计

### 4.1 ER 关系概览

```text
users ─────────┐
  │            │
  │ 1:N        │ 1:N
  ▼            ▼
uploaded_files  contribution_scores
  │
  │ 1:1
  ▼
knowledge_items
  │
  │ (if abnormal)
  ▼
abnormal_cases

agent_messages ← 独立记录表
```

### 4.2 users 表

```sql
CREATE TABLE users (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  username TEXT NOT NULL UNIQUE,
  password TEXT NOT NULL,
  role TEXT NOT NULL,           -- 'employee' | 'admin'
  name TEXT NOT NULL,
  department TEXT,
  position TEXT,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

### 4.3 uploaded_files 表

```sql
CREATE TABLE uploaded_files (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  title TEXT NOT NULL,
  file_name TEXT,
  file_type TEXT NOT NULL,      -- 'video' | 'image' | 'audio' | 'document' | 'text'
  file_url TEXT,                -- 首版为本地 uploads URL；二期可替换为对象存储 URL
  storage_key TEXT,             -- 存储 key；首版可为空，二期接 R2/OSS 时使用
  uploader_id INTEGER,
  uploader_name TEXT,
  device_name TEXT,
  process_name TEXT,
  scene_type TEXT,              -- 枚举见下方
  is_abnormal INTEGER DEFAULT 0,
  risk_level TEXT DEFAULT 'none', -- 'none'|'low'|'medium'|'high'|'critical'
  tags TEXT,                    -- 逗号分隔
  description TEXT,
  text_content TEXT,            -- 纯文本类型时的正文
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

### 4.4 knowledge_items 表

```sql
CREATE TABLE knowledge_items (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  title TEXT NOT NULL,
  knowledge_type TEXT,          -- '标准操作'|'异常案例'|'培训经验'|'故障处理'|'质检记录'|'维修经验'|'其他'
  source_file_id INTEGER,
  device_name TEXT,
  process_name TEXT,
  contributor_id INTEGER,
  contributor_name TEXT,
  tags TEXT,
  status TEXT DEFAULT 'pending', -- 'pending'|'approved'|'rejected'
  summary TEXT,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

### 4.5 contribution_scores 表

```sql
CREATE TABLE contribution_scores (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  user_id INTEGER,
  user_name TEXT,
  action_type TEXT,             -- 'upload_normal'|'upload_video'|'upload_abnormal' 等
  points INTEGER,
  related_file_id INTEGER,
  description TEXT,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

### 4.6 agent_messages 表

```sql
CREATE TABLE agent_messages (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  user_id INTEGER,
  role_type TEXT,
  question TEXT,
  answer TEXT,
  mode TEXT,                    -- 'mock'|'proxy'|'mock_fallback'
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

### 4.7 abnormal_cases 表

```sql
CREATE TABLE abnormal_cases (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  source_file_id INTEGER,
  title TEXT,
  device_name TEXT,
  process_name TEXT,
  risk_level TEXT,
  uploader_id INTEGER,
  uploader_name TEXT,
  description TEXT,
  status TEXT DEFAULT 'pending', -- 'pending'|'processing'|'resolved'
  ai_suggestion TEXT,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

### 4.8 枚举值汇总

**scene_type（场景类型）：**

| 值 | 中文 |
|---|------|
| standard_operation | 标准操作 |
| abnormal_operation | 异常操作 |
| training_experience | 培训经验 |
| fault_case | 故障案例 |
| quality_inspection | 质检记录 |
| maintenance_record | 维修记录 |
| other | 其他 |

**risk_level（风险等级）：**

| 值 | 中文 | 颜色 |
|---|------|------|
| none | 无风险 | 灰色 |
| low | 低风险 | 蓝色/青色 |
| medium | 中风险 | 橙色/黄色 |
| high | 高风险 | 红色 |
| critical | 严重风险 | 深红色 |

**scene_type → knowledge_type 映射：**

| scene_type | knowledge_type |
|---|---|
| standard_operation | 标准操作 |
| abnormal_operation | 异常案例 |
| training_experience | 培训经验 |
| fault_case | 故障处理 |
| quality_inspection | 质检记录 |
| maintenance_record | 维修经验 |
| other | 其他 |

---

## 5. API 接口设计

### 5.1 接口总览

统一前缀：`/api`，认证方式：Bearer Token（`Authorization: Bearer <token>`）

| 方法 | 路径 | 说明 | 认证 |
|------|------|------|------|
| POST | `/api/auth/login` | 登录 | 否 |
| GET | `/api/user/profile` | 获取当前用户信息 | 是 |
| POST | `/api/upload` | 多模态文件上传 | 是 |
| GET | `/api/files` | 文件列表（支持筛选） | 是 |
| GET | `/api/files/{file_id}` | 文件详情 | 是 |
| GET | `/api/knowledge` | 知识条目列表 | 是 |
| GET | `/api/abnormal-cases` | 异常案例列表 | 是 |
| GET | `/api/contributions` | 贡献积分明细 | 是 |
| GET | `/api/dashboard/summary` | 管理端统计摘要 | 是 |
| GET | `/api/dashboard/recent-uploads` | 最近上传（实时流） | 是 |
| GET | `/api/dashboard/ranking` | 贡献排行榜 | 是 |
| GET | `/api/dashboard/graph` | 知识图谱数据 | 是 |
| POST | `/api/agent/chat` | Agent 问答 | 是 |

### 5.2 登录接口

```http
POST /api/auth/login
Content-Type: application/json
```

**请求体：**
```json
{
  "username": "employee",
  "password": "123456"
}
```

**成功响应 (200)：**
```json
{
  "token": "demo-token-employee",
  "user": {
    "id": 1,
    "username": "employee",
    "name": "张三",
    "role": "employee",
    "department": "一号产线",
    "position": "操作员"
  }
}
```

**失败响应 (401)：**
```json
{ "detail": "用户名或密码错误" }
```

### 5.3 文件上传接口

```http
POST /api/upload
Content-Type: multipart/form-data
```

**FormData 字段：**

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| file | File | 是（text 类型除外） | 上传文件 |
| title | string | 是 | 标题 |
| file_type | string | 是 | video/image/audio/document/text |
| device_name | string | 是 | 设备名称 |
| process_name | string | 是 | 工序名称 |
| scene_type | string | 是 | 场景类型枚举 |
| is_abnormal | string | 否 | '0' 或 '1' |
| risk_level | string | 否 | 风险等级枚举 |
| tags | string | 否 | 逗号分隔标签 |
| description | string | 否 | 描述 |
| text_content | string | text 类型时必填 | 纯文本内容 |

**`is_abnormal` 类型约定：**
- 请求 FormData 接受 `'0'` / `'1'` 字符串。
- 后端入库转为 SQLite `INTEGER`：`0` / `1`。
- API 响应统一返回 boolean：`false` / `true`。

**成功响应 (200)：**
```json
{
  "message": "上传成功",
  "file": {
    "id": 1,
    "title": "设备A未闭合安全锁图片",
    "file_type": "image",
    "file_url": "/uploads/20260525_xxx.png",
    "is_abnormal": true,
    "risk_level": "high"
  },
  "knowledge_item": { "id": 1, "title": "...", "status": "pending" },
  "abnormal_case": { "id": 1, "risk_level": "high", "status": "pending" },
  "score_added": 25
}
```

### 5.4 文件列表与详情接口

```http
GET /api/files?user_id=1&file_type=video&is_abnormal=1&keyword=设备A&limit=20&offset=0
```

**响应：**
```json
{
  "items": [
    {
      "id": 1,
      "title": "设备A未闭合安全锁图片",
      "file_name": "lock.png",
      "file_type": "image",
      "file_url": "/uploads/20260525_lock.png",
      "uploader_id": 1,
      "uploader_name": "张三",
      "device_name": "设备A",
      "process_name": "开机检查",
      "scene_type": "abnormal_operation",
      "is_abnormal": true,
      "risk_level": "high",
      "tags": "安全锁,开机,异常",
      "description": "安全锁未闭合",
      "created_at": "2026-05-25T09:30:00"
    }
  ],
  "total": 42
}
```

```http
GET /api/files/{file_id}
```

**响应：** 返回单个文件记录，字段同 `/api/files` 的 `items[]`。

### 5.5 知识条目列表接口

```http
GET /api/knowledge?keyword=设备A&knowledge_type=异常案例&device_name=设备A&status=pending&limit=20&offset=0
```

**响应：**
```json
{
  "items": [
    {
      "id": 1,
      "title": "设备A - 开机检查 - 设备A未闭合安全锁图片",
      "knowledge_type": "异常案例",
      "source_file_id": 1,
      "device_name": "设备A",
      "process_name": "开机检查",
      "contributor_id": 1,
      "contributor_name": "张三",
      "tags": "安全锁,开机,异常",
      "status": "pending",
      "summary": "该知识条目来源于员工张三上传的图片数据...",
      "created_at": "2026-05-25T09:30:00"
    }
  ],
  "total": 42
}
```

### 5.6 最近上传接口

```http
GET /api/dashboard/recent-uploads?limit=10
```

**响应：**
```json
{
  "items": [
    {
      "id": 1,
      "title": "设备A未闭合安全锁图片",
      "file_type": "image",
      "uploader_name": "张三",
      "device_name": "设备A",
      "process_name": "开机检查",
      "is_abnormal": true,
      "risk_level": "high",
      "created_at": "2026-05-25T09:30:00"
    }
  ]
}
```

### 5.7 贡献排行榜接口

```http
GET /api/dashboard/ranking?limit=10
```

**响应：**
```json
{
  "items": [
    {
      "rank": 1,
      "user_id": 1,
      "user_name": "张三",
      "department": "一号产线",
      "upload_count": 18,
      "knowledge_count": 16,
      "abnormal_count": 5,
      "total_points": 260
    }
  ]
}
```

### 5.8 异常案例列表接口

```http
GET /api/abnormal-cases?risk_level=high&status=pending&device_name=设备A&limit=20&offset=0
```

**响应：**
```json
{
  "items": [
    {
      "id": 1,
      "source_file_id": 1,
      "title": "设备A未闭合安全锁图片",
      "device_name": "设备A",
      "process_name": "开机检查",
      "risk_level": "high",
      "uploader_id": 1,
      "uploader_name": "张三",
      "description": "安全锁未闭合",
      "status": "pending",
      "ai_suggestion": "建议立即停止开机流程并由班组长复核安全锁状态。",
      "created_at": "2026-05-25T09:30:00"
    }
  ],
  "total": 18
}
```

### 5.9 贡献积分明细接口

```http
GET /api/contributions?user_id=1&limit=20&offset=0
```

**响应：**
```json
{
  "items": [
    {
      "id": 1,
      "user_id": 1,
      "user_name": "张三",
      "action_type": "upload_abnormal",
      "points": 25,
      "related_file_id": 1,
      "description": "上传高风险异常案例：设备A未闭合安全锁图片",
      "created_at": "2026-05-25T09:30:00"
    }
  ],
  "total": 18,
  "total_points": 260
}
```

### 5.10 管理端统计摘要

```http
GET /api/dashboard/summary
```

**响应：**
```json
{
  "total_files": 1248,
  "today_uploads": 36,
  "total_knowledge": 862,
  "total_abnormal": 57,
  "total_contributors": 42,
  "agent_calls": 319,
  "file_type_distribution": { "video": 320, "image": 480, "audio": 96, "document": 210, "text": 142 },
  "risk_distribution": { "low": 18, "medium": 25, "high": 12, "critical": 2 },
  "daily_trend": [
    { "date": "2026-05-19", "uploads": 22, "knowledge": 18, "abnormal": 3 },
    { "date": "2026-05-20", "uploads": 30, "knowledge": 25, "abnormal": 5 }
  ]
}
```

### 5.11 知识图谱接口

```http
GET /api/dashboard/graph
```

**响应格式：**
```json
{
  "nodes": [
    { "id": "user_1", "name": "张三", "category": "employee" },
    { "id": "device_设备A", "name": "设备A", "category": "device" },
    { "id": "process_开机检查", "name": "开机检查", "category": "process" },
    { "id": "file_1", "name": "设备A开机流程视频", "category": "file" },
    { "id": "knowledge_1", "name": "设备A开机检查流程", "category": "knowledge" }
  ],
  "links": [
    { "source": "user_1", "target": "file_1", "label": "上传" },
    { "source": "file_1", "target": "knowledge_1", "label": "生成" },
    { "source": "knowledge_1", "target": "device_设备A", "label": "关联设备" },
    { "source": "knowledge_1", "target": "process_开机检查", "label": "关联工序" }
  ]
}
```

节点 category 类型：`employee` | `file` | `knowledge` | `device` | `process` | `abnormal`

### 5.12 Agent 聊天接口

```http
POST /api/agent/chat
```

**请求体：**
```json
{
  "user_id": 1,
  "role_type": "operation_qa",
  "question": "设备A开机前需要检查什么？",
  "context": { "device_name": "设备A", "process_name": "开机检查" }
}
```

**响应体：**
```json
{
  "answer": "根据设备A开机前检查规范...",
  "role_type": "operation_qa",
  "mode": "mock",
  "suggestions": ["查看设备A开机检查流程", "查看相关异常案例"],
  "sources": ["模拟SOP：设备A开机检查流程"]
}
```

**role_type 枚举：**

| role_type | 名称 | 所属端 |
|-----------|------|--------|
| training_assistant | 新员工培训助手 | 员工端 |
| operation_qa | 员工操作问答助手 | 员工端 |
| abnormal_alert | 异常操作提醒助手 | 员工端 |
| quality_supervisor | 工作质量监督 | 管理端 |
| management_decision | 管理决策支持 | 管理端 |

---

## 6. 核心业务逻辑

### 6.1 文件上传完整流程

```text
用户选择文件 + 填写元数据
    ↓
前端发送 multipart/form-data 到 POST /api/upload
    ↓
后端处理流程：
    ① 校验用户身份
    ② 保存文件到 backend/uploads/（二期可替换为 R2/OSS）
    ③ 写入 uploaded_files 表
    ④ 自动生成知识条目 → 写入 knowledge_items 表
    ⑤ 判断 is_abnormal → 若 true，写入 abnormal_cases 表
    ⑥ 计算积分 → 写入 contribution_scores 表
    ⑦ 返回完整结果（file + knowledge_item + score_added）
    ↓
前端处理：
    ① 显示上传成功提示 + 积分增加
    ② 刷新最近上传记录
    ③ 管理端刷新实时数据流
```

### 6.2 知识条目自动生成规则

**标题生成模板：**
```text
{设备名称} - {工序名称} - {上传标题}
例：设备A - 开机检查 - 设备A开机前检查流程
```

**知识类型映射：** scene_type → knowledge_type（见 4.8 节映射表）

**摘要生成模板（Demo 阶段不调用 AI）：**
```text
该知识条目来源于员工 {贡献人} 上传的 {文件类型} 数据，
关联设备为 {设备名称}，关联工序为 {工序名称}，标签为 {标签}。
```

**状态默认值：** `pending`（上传后待审核，增加真实感）

### 6.3 积分计算规则

```python
def calculate_points(file):
    points = 5                          # 基础分

    if file.file_type == "video":
        points += 5                     # 视频额外 +5

    if file.scene_type in ["standard_operation", "training_experience"]:
        points += 5                     # 标准操作/培训经验 +5

    if file.is_abnormal:
        points += 10                    # 异常案例 +10

    if file.risk_level == "high":
        points += 5                     # 高风险 +5

    if file.risk_level == "critical":
        points += 10                    # 严重风险 +10

    return points
```

**积分参考表：**

| 场景 | 积分 |
|------|------|
| 上传普通文件 | 5 |
| 上传视频 | 10 |
| 上传标准操作/培训经验视频 | 15 |
| 上传异常案例 | 15 |
| 上传高风险异常 | 20 |
| 上传严重风险异常 | 25 |

### 6.4 异常案例自动生成规则

当 `is_abnormal = true` 时自动创建 abnormal_cases 记录。

**ai_suggestion 模板：**

- 一般异常：`该异常与 {设备名称} 的 {工序名称} 环节相关，建议班组长复核该操作，并将相关经验纳入培训材料。`
- 高风险/严重：`该异常风险等级较高，建议暂停相关操作，完成现场复核后再恢复生产。`

### 6.5 知识图谱动态生成规则

图谱不需要单独存表，从最近 N 条 uploaded_files + knowledge_items 动态组装。

**节点类型：**

| 类型 | 来源 | 颜色（管理端） |
|------|------|---------------|
| employee | users 表 | `#34D399` 绿 |
| file | uploaded_files 表 | 灰色 |
| knowledge | knowledge_items 表 | `#22D3EE` 青 |
| device | uploaded_files.device_name 去重 | `#3B82F6` 蓝 |
| process | uploaded_files.process_name 去重 | `#8B5CF6` 紫 |
| abnormal | abnormal_cases 表 | `#F87171` 红 |

**边关系：**

| 关系 | 说明 |
|------|------|
| employee → file | 上传 |
| file → knowledge | 生成 |
| knowledge → device | 关联设备 |
| knowledge → process | 关联工序 |
| abnormal → process | 异常发生于 |

### 6.6 Agent 双模式设计

```text
AGENT_MODE=mock   → 根据 role_type 返回模板答案，不调用外部 API
AGENT_MODE=proxy  → 读取 AGENT_API_KEY，调用 OpenAI-Compatible Chat Completions API
                    失败时自动降级到 mock（mode 标记为 mock_fallback）
```

**proxy 模式调用流程：**
```text
① 接收前端问题
② 从数据库查询上传记录/异常案例/知识条目摘要
③ 拼接 system prompt（根据 role_type 选择不同模板）
④ 读取 AGENT_API_BASE_URL + AGENT_API_KEY + AGENT_MODEL
⑤ POST {AGENT_API_BASE_URL}/chat/completions
⑥ 解析返回结果
⑦ 保存对话到 agent_messages 表
⑧ 返回给前端
```

**API Key 安全原则：**
- API Key 只存在后端环境变量中
- 前端只请求 `/api/agent/chat`
- `.env` 文件必须在 `.gitignore` 中
- 日志中只显示 Key 的最后 4 位（如 `sk-****abcd`）
- Key 未配置时自动降级 mock

---

## 7. 前端设计规范

### 7.1 双主题系统

系统采用双主题设计，员工端和管理端视觉风格截然不同。

#### 7.1.1 员工端 — 马卡龙配色（简洁亲和风）

设计关键词：简洁、明亮、轻量、低压迫感、操作按钮清晰、适合平板/手机/工位电脑

**配色表：**

| 用途 | 色值 | CSS 变量建议 |
|------|------|-------------|
| 页面背景 | `#F8FAFC` | `--emp-bg` |
| 主色（按钮/重点） | `#A7C7E7` | `--emp-primary` |
| 辅助色1（成功/积分） | `#B8E0D2` | `--emp-success` |
| 辅助色2（温和提示） | `#F7D6E0` | `--emp-info` |
| 辅助色3（提醒） | `#FFF1B8` | `--emp-warning` |
| 文本主色 | `#334155` | `--emp-text` |
| 文本次色 | `#64748B` | `--emp-text-secondary` |
| 卡片背景 | `#FFFFFF` | `--emp-card` |
| 边框 | `#E2E8F0` | `--emp-border` |
| 危险色 | `#FCA5A5` | `--emp-danger` |

**关键规则：**
- 不使用大面积黑色、深蓝、强紫、霓虹光效
- 卡片圆角 `16px`，登录卡片圆角 `20px`
- 阴影轻柔，使用低对比度
- 大按钮、少步骤、低学习成本

#### 7.1.2 管理端 — 蓝紫科技风（数据大屏风）

设计关键词：蓝紫配色、深色背景、科技大屏、发光边框、数据驾驶舱、适合路演投屏

**配色表：**

| 用途 | 色值 | CSS 变量建议 |
|------|------|-------------|
| 页面背景 | `#050816` | `--adm-bg` |
| 背景辅助 | `#0F172A` | `--adm-bg-secondary` |
| 主色 | `#3B82F6` | `--adm-primary` |
| 辅助紫 | `#8B5CF6` | `--adm-purple` |
| 高亮青 | `#22D3EE` | `--adm-highlight` |
| 成功绿 | `#34D399` | `--adm-success` |
| 警告黄 | `#FBBF24` | `--adm-warning` |
| 危险红 | `#F87171` | `--adm-danger` |
| 主文本 | `#E5E7EB` | `--adm-text` |
| 次文本 | `#94A3B8` | `--adm-text-secondary` |
| 边框光效 | `rgba(59,130,246,0.35)` | `--adm-glow` |

**背景渐变：**
```css
background: radial-gradient(circle at top left, rgba(59,130,246,0.25), transparent 32%),
            radial-gradient(circle at top right, rgba(139,92,246,0.22), transparent 30%),
            #050816;
```

**关键规则：**
- 深色半透明卡片 + 蓝紫渐变边框
- 数字使用大字号，带轻微发光效果
- 排行榜 Top 1/2/3 分别使用金/银/铜色高亮
- 实时数据流使用终端日志风格（时间用青色，异常用红色标签）

### 7.2 前端项目目录结构

```text
frontend/
├── package.json
├── vite.config.ts
├── index.html
├── .env.example
├── .env.development
├── .env.production            # 二期/部署备选，首版可不创建
├── src/
│   ├── main.tsx
│   ├── App.tsx
│   ├── router/
│   │   └── index.tsx
│   ├── api/
│   │   ├── request.ts          # Axios 实例 + 拦截器
│   │   ├── auth.ts
│   │   ├── upload.ts
│   │   ├── files.ts
│   │   ├── dashboard.ts
│   │   ├── knowledge.ts
│   │   ├── abnormal.ts
│   │   ├── contributions.ts
│   │   └── agent.ts
│   ├── store/
│   │   └── userStore.ts
│   ├── layouts/
│   │   ├── EmployeeLayout.tsx   # 马卡龙主题布局
│   │   └── AdminLayout.tsx      # 蓝紫科技风布局
│   ├── pages/
│   │   ├── Login.tsx
│   │   ├── employee/
│   │   │   ├── Dashboard.tsx
│   │   │   ├── Upload.tsx
│   │   │   ├── Assistant.tsx
│   │   │   └── Contribution.tsx
│   │   └── admin/
│   │       ├── Dashboard.tsx
│   │       ├── Knowledge.tsx
│   │       ├── Graph.tsx
│   │       ├── Abnormal.tsx
│   │       ├── Ranking.tsx
│   │       └── DecisionAgent.tsx
│   ├── components/
│   │   ├── StatCard.tsx
│   │   ├── FileTypeTag.tsx
│   │   ├── RiskTag.tsx
│   │   ├── UploadForm.tsx
│   │   ├── RecentUploadList.tsx
│   │   ├── ContributionRanking.tsx
│   │   ├── KnowledgeGraph.tsx
│   │   └── AgentChat.tsx
│   ├── types/
│   │   └── index.ts
│   └── styles/
│       ├── global.css
│       ├── employee-theme.css
│       └── admin-theme.css
```

### 7.3 前端环境变量

首版 Agent B 只需要创建 `frontend/.env.example` 和按需创建 `frontend/.env.development`。`.env.production` 示例仅供二期部署参考，首版并行开发不得因这些示例创建 Cloudflare 或 GitHub Pages 部署文件。

```env
# .env.example
VITE_API_BASE_URL=http://localhost:8000
VITE_APP_TITLE=工业现场多模态智能管理平台
VITE_DEMO_MODE=false
```

```env
# .env.production（二期 Cloudflare Pages 备选）
VITE_API_BASE_URL=https://industrial-demo-api.xxx.workers.dev
VITE_APP_TITLE=工业现场多模态智能管理平台
VITE_DEMO_MODE=false
```

```env
# .env.production（应急 GitHub Pages 静态演示备选）
VITE_API_BASE_URL=
VITE_APP_TITLE=工业现场多模态智能管理平台
VITE_DEMO_MODE=true
```

### 7.4 支持的文件类型

| 类型 | 后缀 | 预览方式 |
|------|------|----------|
| 图片 | jpg, jpeg, png, webp | 缩略图 |
| 视频 | mp4, mov, avi | 视频图标 + 文件名 |
| 音频 | mp3, wav, m4a | 音频图标 |
| 文档 | pdf, doc, docx, txt | 文档图标 |
| 文本 | 直接输入 | 无文件上传 |

---

## 8. 后端设计规范

### 8.1 首版 MVP — FastAPI 项目结构

```text
backend/
├── requirements.txt
├── main.py                    # FastAPI 入口
├── .env.example
├── .env
├── .gitignore
├── uploads/                   # 上传文件存储
├── app/
│   ├── __init__.py
│   ├── database.py            # SQLAlchemy 引擎 + 会话
│   ├── models.py              # ORM 模型
│   ├── schemas.py             # Pydantic 请求/响应模型
│   ├── seed.py                # 初始化 seed 数据
│   ├── routers/
│   │   ├── auth.py            # 登录
│   │   ├── user.py            # 用户信息
│   │   ├── upload.py          # 文件上传
│   │   ├── files.py           # 文件列表/详情
│   │   ├── knowledge.py       # 知识条目
│   │   ├── abnormal.py        # 异常案例
│   │   ├── contributions.py   # 贡献积分
│   │   ├── dashboard.py       # 管理端统计
│   │   └── agent.py           # Agent 聊天
│   └── services/
│       ├── file_service.py    # 文件保存逻辑
│       ├── knowledge_service.py  # 知识条目生成
│       ├── score_service.py   # 积分计算
│       ├── graph_service.py   # 图谱数据组装
│       └── agent_service.py   # Agent mock/proxy
```

### 8.2 二期备选 — Cloudflare Workers 项目结构

本节仅作为二期迁移参考，不进入首版 MVP 并行开发范围。首版 Agent 任务中不得创建 `worker/` 或相关配置文件，除非后续收到新的明确指令。

```text
worker/
├── package.json
├── wrangler.toml
├── tsconfig.json
├── .dev.vars                  # 本地开发环境变量（不提交）
├── .gitignore
├── migrations/
│   └── 0001_init.sql          # D1 初始化 SQL
└── src/
    ├── index.ts               # Hono 入口
    ├── routes/
    │   ├── auth.ts
    │   ├── upload.ts
    │   ├── files.ts
    │   ├── knowledge.ts
    │   ├── abnormal.ts
    │   ├── contributions.ts
    │   ├── dashboard.ts
    │   └── agent.ts
    └── utils/
        ├── response.ts        # 统一响应格式
        ├── auth.ts            # Token 校验
        └── agent.ts           # Agent mock/proxy 逻辑
```

### 8.3 后端环境变量（首版 MVP）

```env
# backend/.env.example
APP_ENV=development
APP_HOST=0.0.0.0
APP_PORT=8000
DATABASE_URL=sqlite:///./industrial_demo.db
UPLOAD_DIR=uploads
MAX_UPLOAD_SIZE_MB=200

# Agent 模式
AGENT_MODE=mock
AGENT_API_BASE_URL=https://api.openai.com/v1
AGENT_API_KEY=your_api_key_here
AGENT_MODEL=gpt-4o-mini
AGENT_TIMEOUT_SECONDS=60

# CORS
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
```

### 8.4 Cloudflare Workers 配置（二期备选）

本节仅在决定切换到 Cloudflare 路线后执行，首版 Demo 不要求创建 `worker/`。首版 Agent 任务中不得创建相关文件，除非后续收到新的明确指令。

**wrangler.toml：**
```toml
name = "industrial-demo-api"
main = "src/index.ts"
compatibility_date = "2026-05-25"

[[d1_databases]]
binding = "DB"
database_name = "industrial-demo-db"
database_id = "替换为你的D1数据库ID"

[[r2_buckets]]
binding = "UPLOAD_BUCKET"
bucket_name = "industrial-demo-uploads"

[vars]
AGENT_API_BASE_URL = "https://api.openai.com/v1"
AGENT_MODEL = "gpt-4o-mini"
AGENT_MODE = "proxy"
```

API Key 通过 secret 设置：
```bash
npx wrangler secret put AGENT_API_KEY
```

### 8.5 Agent 系统提示词设计

每种 role_type 有独立的 system prompt：

| role_type | 核心指令摘要 |
|-----------|-------------|
| training_assistant | 为新员工提供分步骤学习建议，包含标准操作/安全/常见错误/推荐内容 |
| operation_qa | 帮助一线员工解决操作问题，优先提示安全风险 |
| abnormal_alert | 判断异常风险等级(low/medium/high)，给出立即处理建议 |
| quality_supervisor | 基于数据分析操作质量风险点、原因、改进建议 |
| management_decision | 为管理层提供决策建议：结论+依据+风险+优先级+行动 |

### 8.6 Agent Mock 模板答案

| role_type | 模板答案（截取） |
|-----------|----------------|
| training_assistant | "建议新员工先学习设备结构、安全规范、标准开机流程和常见异常案例，再进行实操。" |
| operation_qa | "建议按照 SOP 先检查设备状态、确认安全锁、查看报警代码..." |
| abnormal_alert | "该情况可能属于中风险异常，建议立即停止当前操作并通知班组长复核。" |
| quality_supervisor | "当前操作质量风险主要来自流程跳步、检查项遗漏..." |
| management_decision | "从当前数据看，异常主要集中在开机前检查和上料确认环节..." |

---

## 9. Demo 数据设计

### 9.1 预置设备

| 编号 | 设备名称 | 说明 |
|------|---------|------|
| 1 | 设备A | 自动上料机 |
| 2 | 设备B | 轧制设备 |
| 3 | 设备C | 质检视觉工位 |
| 4 | 设备D | 包装机械臂 |

### 9.2 预置工序

| 工序 |
|------|
| 开机检查 |
| 上料操作 |
| 运行监控 |
| 异常停机处理 |
| 产品质检 |
| 设备维护 |
| 安全复核 |

### 9.3 预置员工

| username | name | department | position |
|----------|------|-----------|----------|
| employee | 张三 | 一号产线 | 设备操作员 |
| admin | 李经理 | 生产管理部 | 管理人员 |
| worker2 | 王师傅 | 二号产线 | 资深操作员 |
| worker3 | 赵工 | 维修班组 | 维修工程师 |
| worker4 | 刘班长 | 一号产线 | 班组长 |

### 9.4 示例上传记录（seed 数据）

| # | 标题 | 文件类型 | 设备 | 工序 | 场景 | 异常 | 风险 |
|---|------|---------|------|------|------|------|------|
| 1 | 设备A开机前安全锁检查视频 | video | 设备A | 开机检查 | standard_operation | 否 | none |
| 2 | 设备B运行中报警面板截图 | image | 设备B | 运行监控 | abnormal_operation | 是 | medium |
| 3 | 轧制设备润滑系统维护经验 | document | 设备B | 设备维护 | maintenance_record | 否 | none |
| 4 | 新员工上料操作培训录音 | audio | 设备A | 上料操作 | training_experience | 否 | none |
| 5 | 产品表面划痕质检图片 | image | 设备C | 产品质检 | quality_inspection | 是 | low |
| 6 | 机械臂异常停机处理记录 | document | 设备D | 异常停机处理 | fault_case | 是 | high |
| 7 | 未佩戴手套错误操作图片 | image | 设备A | 上料操作 | abnormal_operation | 是 | high |
| 8 | 包装工位标准作业流程 | document | 设备D | 运行监控 | standard_operation | 否 | none |

seed 数据需要同时生成对应的 knowledge_items、contribution_scores、abnormal_cases 记录。

### 9.5 seed 数据量目标

为保证大屏展示效果，seed 数据量建议：

| 指标 | 目标数量 |
|------|---------|
| 总上传文件 | 50-80 条 |
| 知识条目 | 45-70 条 |
| 异常案例 | 15-20 条 |
| 贡献积分记录 | 50-80 条 |
| Agent 对话 | 10-15 条 |

---

## 10. 部署方案

### 10.1 本地开发（首版必做）

```bash
# 后端
cd backend
python -m venv venv
venv\Scripts\activate          # Windows
pip install -r requirements.txt
uvicorn main:app --reload --host 0.0.0.0 --port 8000

# 前端
cd frontend
npm install
npm run dev
```

访问地址：
- 前端：http://localhost:5173
- 后端 API 文档：http://localhost:8000/docs

### 10.2 Cloudflare 全栈部署（二期备选）

首版 Demo 不执行本节。只有当本地版本完成并需要公网无服务器演示时，才创建 `worker/`、D1、R2 和 Cloudflare Pages 项目。首版 Agent 任务中不得创建相关文件，除非后续收到新的明确指令。

#### 步骤一：准备

```bash
npm install -g wrangler
wrangler login
```

#### 步骤二：创建 D1 数据库

```bash
cd worker
npx wrangler d1 create industrial-demo-db
# 把返回的 database_id 写入 wrangler.toml
npx wrangler d1 migrations apply industrial-demo-db --remote
```

#### 步骤三：创建 R2 Bucket

```bash
npx wrangler r2 bucket create industrial-demo-uploads
```

#### 步骤四：设置 Secret

```bash
npx wrangler secret put AGENT_API_KEY
# 输入真实 API Key
```

#### 步骤五：部署 Worker

```bash
cd worker
npm install
npm run deploy
# 得到 https://industrial-demo-api.xxx.workers.dev
```

#### 步骤六：部署前端

```bash
cd frontend
# 修改 .env.production 中 VITE_API_BASE_URL 为 Worker 地址
npm install
npm run build
npx wrangler pages deploy dist --project-name industrial-demo-web
```

### 10.3 GitHub Pages 静态演示（备选）

首版 Demo 不执行本节。首版 Agent 任务中不得创建 GitHub Pages 相关配置或发布脚本，除非后续收到新的明确指令。

适用场景：纯 UI 展示、无后端、无真实上传、无 API Key 保护。

```bash
cd frontend
# 确保 .env.production 中 VITE_DEMO_MODE=true
npm install
npm run build
npx gh-pages -d dist
```

**DEMO_MODE=true 行为：**
- 登录使用固定账号验证（前端内存）
- 上传数据保存到 localStorage
- 图谱从 localStorage 动态生成
- Agent 返回固定 mock 答案
- 刷新后本机数据保留，换电脑不共享

**vite.config.ts 需配置 base：**
```ts
base: '/industrial-demo/'
```

### 10.4 有服务器部署（可选生产方案）

首版 Demo 不执行本节。首版 Agent 任务中不得创建 `deploy/`、Nginx 或 systemd 相关文件，除非后续收到新的明确指令。

如果后续获得云服务器，可使用 Nginx + systemd 部署：

```text
服务器要求：Ubuntu 22.04, 2核CPU, 4GB内存, 40GB磁盘
开放端口：22(SSH), 80(HTTP), 443(HTTPS)
```

```text
/opt/industrial-multimodal-demo/
├── frontend/dist/              # Nginx 托管
├── backend/
│   ├── .env
│   ├── uploads/
│   ├── industrial_demo.db
│   └── venv/
└── deploy/
    ├── nginx.conf
    ├── industrial-demo.service
    └── deploy.sh
```

Nginx 核心配置：
```nginx
location / { try_files $uri $uri/ /index.html; }
location /api/ { proxy_pass http://127.0.0.1:8000/api/; }
location /uploads/ { proxy_pass http://127.0.0.1:8000/uploads/; }
```

---

## 11. 开发阶段划分与任务清单

### 阶段 0：并行开发前置锁定（两人共同确认）

**目标：** 让后端和前端可以同时开工，不互相等待。

| # | 任务 | 负责人 | 产出 |
|---|------|--------|------|
| 0.1 | 锁定首版只做 FastAPI + SQLite + 本地 uploads | 两人共同 | 不创建 `worker/`，不做 Cloudflare/D1/R2 |
| 0.2 | 锁定 API 契约 | 两人共同 | 以第 5 节接口、字段、枚举、响应结构为准 |
| 0.3 | 锁定目录所有权 | 两人共同 | Agent A 只改 `backend/`；Agent B 只改 `frontend/` |
| 0.4 | 锁定共享文件策略 | 两人共同 | 根 `README.md`、根 `.gitignore` 由集成负责人统一处理；`deploy/` 首版不创建 |
| 0.5 | 锁定联调方式 | 两人共同 | 后端跑 `http://localhost:8000`，前端跑 `http://localhost:5173` |

**阶段 0 验收标准：**
- 两名 Agent 都能看到本文档。
- 两名 Agent 都确认自己的文件边界。
- 任何 API 字段变更都先改本文档，再改代码。

### 并行主线 A：后端与数据层（Agent A）

**目标：** 提供稳定的本地 API、数据库、文件上传和 mock/proxy Agent 能力。

| # | 任务 | 产出 |
|---|------|------|
| A1 | 创建 FastAPI 项目框架 | `backend/main.py`、`backend/app/`、`backend/requirements.txt` |
| A2 | 实现数据库层 | `backend/app/database.py`、`backend/app/models.py` |
| A3 | 实现 Pydantic schema | `backend/app/schemas.py`，字段与第 4-5 节一致 |
| A4 | 实现 seed 初始化 | `backend/app/seed.py`，预置账号、设备、工序、50-80 条演示数据 |
| A5 | 实现认证接口 | `POST /api/auth/login`、`GET /api/user/profile` |
| A6 | 实现上传闭环 | `POST /api/upload`，保存文件、生成知识、积分、异常案例 |
| A7 | 实现查询接口 | `/api/files`、`/api/files/{file_id}`、`/api/knowledge`、`/api/abnormal-cases`、`/api/contributions` |
| A8 | 实现 dashboard 接口 | summary、recent-uploads、ranking、graph |
| A9 | 实现 Agent 接口 | `POST /api/agent/chat`，默认 mock，支持 proxy fallback |
| A10 | 配置 CORS 和 uploads 静态访问 | 前端可访问 API 和上传文件 URL |
| A11 | 后端自测 | 用 `/docs` 或脚本验证所有接口 |

**Agent A 阶段验收标准：**
- `python -m venv venv`、`pip install -r requirements.txt`、`uvicorn main:app --reload --host 0.0.0.0 --port 8000` 可启动。
- `http://localhost:8000/docs` 能看到所有接口。
- `employee/123456`、`admin/123456` 能登录。
- 上传一次异常图片后，`uploaded_files`、`knowledge_items`、`contribution_scores`、`abnormal_cases` 都新增记录。
- dashboard、ranking、graph 数据会随上传变化。
- 未配置 `AGENT_API_KEY` 时 Agent 返回 mock，不报错。

### 并行主线 B：前端与演示体验（Agent B）

**目标：** 在后端未完成时先用 mock/fixture 完成页面，后端完成后切换真实 API 联调。

| # | 任务 | 产出 |
|---|------|------|
| B1 | 创建 React + Vite + TypeScript 项目 | `frontend/` 项目骨架 |
| B2 | 安装和配置依赖 | Ant Design、ECharts、Axios、React Router、dayjs、状态管理 |
| B3 | 定义前端类型和 API 层 | `frontend/src/types/`、`frontend/src/api/`，字段与第 5 节一致 |
| B4 | 实现 mock/fixture 数据 | 后端未完成时页面可独立演示 |
| B5 | 实现登录和路由守卫 | `/login`、角色分流、刷新保持登录 |
| B6 | 实现双端布局与主题 | EmployeeLayout 马卡龙，AdminLayout 蓝紫科技风 |
| B7 | 实现员工端页面 | dashboard、upload、assistant、contribution |
| B8 | 实现管理端页面 | dashboard、knowledge、graph、abnormal、ranking、decision-agent |
| B9 | 实现图表和图谱 | ECharts 趋势图、Graph 力导向图 |
| B10 | 实现体验状态 | loading、空状态、错误提示、上传进度、成功积分反馈 |
| B11 | 前端自测 | 无后端 mock 模式可演示，有后端时真实 API 可联调 |

**Agent B 阶段验收标准：**
- `npm install`、`npm run dev` 可启动。
- 访问 `http://localhost:5173/login` 能看到登录页。
- employee 登录进入 `/employee/dashboard`，admin 登录进入 `/admin/dashboard`。
- 无后端时页面不空白，能展示 mock 数据。
- 后端可用时设置 `VITE_API_BASE_URL=http://localhost:8000` 能完成真实登录、上传、统计刷新。
- 管理端大屏、知识图谱、异常案例、排行榜具备可演示视觉效果。

### 集成阶段：两条线合并验收

**目标：** 完成首版 5 分钟 Demo 闭环。

| # | 任务 | 负责人 | 产出 |
|---|------|--------|------|
| I1 | 后端启动并重置 seed 数据 | Agent A | `backend/industrial_demo.db` 自动初始化 |
| I2 | 前端切真实 API | Agent B | `.env.development` 指向 `http://localhost:8000` |
| I3 | 跑通上传闭环 | 两人共同 | 员工上传后管理端数据刷新 |
| I4 | 跑通 Agent 问答 | 两人共同 | mock 模式稳定返回，proxy 模式可选 |
| I5 | 补齐共享工程文件 | 两人共同指定一名集成负责人执行 | 根 `.gitignore`、`README.md`、启动说明；`backend/.env.example` 属于 Agent A，`frontend/.env.example` 属于 Agent B |
| I6 | 完整演示测试 | 两人共同 | 按第 12 节 5 分钟脚本走通 |

**集成验收标准：**
- 前后端同时启动后，演示脚本从登录、上传、管理大屏、知识图谱、排行、Agent 决策完整走通。
- 控制台和终端无阻塞性报错。
- API Key 不出现在前端代码和提交文件中。
- 首版不要求 Cloudflare、GitHub Pages、Nginx/systemd 部署成功。

---

## 12. 演示脚本

### 5 分钟路演主线

```text
第 1 分钟：登录 + 员工端概览
────────────────────────────
1. 打开系统首页（登录页）
2. 使用员工账号 employee/123456 登录
3. 展示员工首页：今日上传、积分、AI问答次数
4. 指出四个快捷入口

第 2 分钟：核心功能 — 多模态上传
────────────────────────────
5. 点击"上传现场数据"
6. 上传一张"设备A未闭合安全锁"的图片
7. 填写元数据：
   - 设备：设备A
   - 工序：开机检查
   - 场景：异常操作
   - 风险等级：high
   - 标签：安全锁、开机、异常
8. 点击上传
9. 展示成功提示：
   ✓ 上传成功
   ✓ 已生成知识条目
   ✓ 积分 +20
   ✓ 异常案例已同步

第 3 分钟：切换管理端 — 数据大屏
────────────────────────────
10. 切换到管理员账号（或直接切换端）
11. 展示管理驾驶舱：
    - 今日新增上传 +1
    - 异常案例数量 +1
    - 知识条目 +1
12. 展示实时数据流中出现新记录（红色异常标签）
13. 展示 7 天趋势图

第 4 分钟：知识图谱 + 贡献排行
────────────────────────────
14. 打开知识图谱页面
15. 展示新节点：
    张三 → 上传 → 异常图片 → 生成 → 异常案例 → 关联设备A/开机检查
16. 鼠标悬停展示节点详情
17. 打开贡献排行榜
18. 展示张三积分提升

第 5 分钟：AI 决策 + 总结
────────────────────────────
19. 打开管理决策 Agent 页面
20. 提问："最近哪个工序异常最多？"
21. Agent 返回结构化建议：
    - 结论：异常集中在开机检查环节
    - 依据：近7天相关异常8条，占42%
    - 建议：加强新员工培训+班组长复核
22. 总结系统价值（一页 PPT 或口述）：
    ① 多模态数据入口
    ② 知识自动沉淀
    ③ 异常可视化管理
    ④ 员工贡献可量化
    ⑤ AI Agent 辅助决策
    ⑥ 后续可快速接入真实 AI 模型
```

---

## 13. 验收清单

### 13.1 功能验收

- [ ] 能登录员工端（employee/123456）
- [ ] 能登录管理端（admin/123456）
- [ ] 员工能上传图片
- [ ] 员工能上传视频
- [ ] 员工能上传音频
- [ ] 员工能上传文档
- [ ] 员工能提交纯文本经验
- [ ] 上传后文件被正确保存
- [ ] 上传后的 `file_url` 可通过浏览器访问 `/uploads/...`
- [ ] 上传后元数据入库
- [ ] 上传后自动生成知识条目
- [ ] 上传后自动增加积分
- [ ] 异常上传能进入异常案例表
- [ ] 新上传记录能同时出现在最近上传、知识库、知识图谱、异常案例和排行榜变化中
- [ ] 管理端能看到统计数据
- [ ] 管理端能看到实时上传流
- [ ] 管理端能看到知识库列表
- [ ] 管理端能看到知识图谱
- [ ] 管理端能看到贡献排行榜
- [ ] 管理端能看到异常案例
- [ ] Agent 问答能返回结果（mock 模式）
- [ ] Agent 问答能返回结果（proxy 模式，Key 配置正确时）
- [ ] Agent API 失败时自动降级 mock

### 13.2 展示验收

- [ ] 管理端首页具有数据大屏感（深色+发光+蓝紫）
- [ ] 员工端简洁亲和（马卡龙浅色）
- [ ] 两端视觉差异明显
- [ ] 指标卡片数字清晰
- [ ] 知识图谱节点颜色分类明确
- [ ] 风险等级颜色区分明显
- [ ] 上传成功反馈明显
- [ ] 页面无明显空白区域
- [ ] 初始 seed 数据丰富
- [ ] Demo 流程 5 分钟内可完整讲完

### 13.3 工程验收

- [ ] 前后端能分别启动
- [ ] 前端 `npm run build` 至少通过一次
- [ ] 前端 API 地址可通过环境变量配置
- [ ] 后端上传目录自动创建
- [ ] SQLite 数据库自动初始化
- [ ] README 完整（启动/配置/账号；首版部署只要求本地启动说明）
- [ ] 无硬编码绝对路径
- [ ] 无明显控制台报错
- [ ] API Key 不在前端代码中出现
- [ ] .env 文件在 .gitignore 中
- [ ] 提供 `backend/.env.example` 和 `frontend/.env.example`
- [ ] 首版验收不检查 `worker/`、`deploy/`、Cloudflare、GitHub Pages、Nginx/systemd

---

## 14. 后续扩展方向

| 优先级 | 扩展项 | 说明 |
|--------|--------|------|
| P0 | 接入真实大模型 Agent API | 配置 AGENT_MODE=proxy + 真实 API Key |
| P1 | 接入真实 RAG 向量数据库 | 替换 mock 知识检索 |
| P1 | 使用 MinIO/云 OSS 存储 | 替换本地 uploads |
| P2 | Neo4j 构建真实知识图谱 | 替换 ECharts 模拟 |
| P2 | PaddleOCR 图片文字识别 | 上传图片自动提取文字 |
| P2 | Whisper 语音转文字 | 上传音频自动转录 |
| P3 | 视频抽帧 + 多模态模型 | 视频内容理解 |
| P3 | 实时摄像头监控接入 | 实时视频流分析 |
| P3 | 管理员审核流程 | 知识条目审核工作流 |
| P4 | 企业微信/钉钉提醒 | 异常即时通知 |
| P4 | 工单系统 | 异常处理闭环 |
| P4 | 真实权限管理 | RBAC 权限体系 |
| P5 | 操作质量评分模型 | 视频行为分析打分 |
| P5 | 员工培训路径生成 | 个性化学习推荐 |
| P5 | SOP 版本管理 | 标准作业流程迭代 |

---

## 15. 双 Agent 并行开发分工

### 15.1 全局协作规则

两名 Agent 开发前都必须先阅读本文档全文，尤其是第 0、4、5、6、7、8、9、11、13、15 节。

**首版共同目标：**
- 在本地完成一个可稳定演示 5 分钟的工业现场多模态智能管理平台 Demo。
- 后端使用 `FastAPI + SQLite + 本地 uploads/`。
- 前端使用 `React + Vite + TypeScript`。
- 默认 Agent 问答使用 mock；有 API Key 时才启用 proxy。

**首版不做：**
- 不做 Cloudflare Workers、D1、R2。
- 不做 GitHub Pages 静态演示。
- 不做 Nginx/systemd 生产部署。
- 不做真实 RAG、真实知识图谱数据库、模型训练。

**文件所有权：**
- Agent A 只负责 `backend/`。
- Agent B 只负责 `frontend/`。
- 根目录共享文件（`README.md`、根 `.gitignore`）先不要动，集成阶段再由双方确认后指定一名集成负责人统一补齐。
- `deploy/`、`worker/` 首版不创建。
- 任何一方不得修改对方目录；如发现必须改对方目录，先在交接说明中提出，不直接改。

**API 契约：**
- API 路径、请求字段、响应字段、枚举值以第 4-6 节为准。
- 如实现中确需调整字段，必须先同步修改本文档，再通知另一方。
- 前端可先用 mock/fixture 按同一契约开发，后端完成后切真实 API。

### 15.2 给 Agent A（后端开发）的提示词

```text
你负责开发“工业现场多模态数据采集与智能管理平台 Demo”的后端部分。

请先阅读全局计划文件：
D:\Deep_learning_project\Demo\工业现场多模态智能管理平台_详细设计计划.md

重点阅读第 0、4、5、6、8、9、11、13、15 节。你的工作必须服从全局计划，并与前端 Agent B 并行开发。

一、全局目标
- 首版 MVP 只做本地 Demo：FastAPI + SQLite + 本地 backend/uploads/。
- 提供员工端和管理端需要的全部 API。
- 支持登录、用户信息、文件上传、文件列表、知识条目、dashboard 统计、最近上传、排行榜、知识图谱、Agent chat。
- 上传后必须自动生成 knowledge_items、contribution_scores；异常上传还要生成 abnormal_cases。
- Agent 默认 mock；配置 AGENT_MODE=proxy 且存在 AGENT_API_KEY 时再调用外部 OpenAI-Compatible API，失败自动 fallback 到 mock。

二、严格文件边界
- 你只允许创建和修改 backend/ 目录内文件。
- 不要修改 frontend/。
- 不要修改 README.md、根 .gitignore、deploy/、worker/。
- 不要创建 Cloudflare Workers、D1、R2、GitHub Pages、Nginx/systemd 相关实现。

三、建议目录
backend/
├── requirements.txt
├── main.py
├── .env.example
├── app/
│   ├── __init__.py
│   ├── database.py
│   ├── models.py
│   ├── schemas.py
│   ├── seed.py
│   ├── routers/
│   │   ├── auth.py
│   │   ├── user.py
│   │   ├── upload.py
│   │   ├── files.py
│   │   ├── knowledge.py
│   │   ├── abnormal.py
│   │   ├── contributions.py
│   │   ├── dashboard.py
│   │   └── agent.py
│   └── services/
│       ├── file_service.py
│       ├── knowledge_service.py
│       ├── score_service.py
│       ├── graph_service.py
│       └── agent_service.py
└── uploads/

四、API 契约
- 严格按计划第 5 节实现。
- 统一前缀 /api。
- 登录接口：POST /api/auth/login。
- 用户信息接口：GET /api/user/profile。
- 上传接口：POST /api/upload。
- 文件接口：GET /api/files、GET /api/files/{file_id}。
- 异常案例接口：GET /api/abnormal-cases。
- 贡献明细接口：GET /api/contributions。
- 知识接口：GET /api/knowledge。
- dashboard 接口：GET /api/dashboard/summary、/recent-uploads、/ranking、/graph。
- Agent 接口：POST /api/agent/chat。
- token 可采用 Demo 简化机制，例如 demo-token-employee、demo-token-admin，但前端必须可通过 Authorization: Bearer <token> 调用受保护接口。

五、数据和业务规则
- 建表字段按计划第 4 节。
- 枚举值按计划第 4.8 节。
- 上传流程按计划第 6.1 节。
- 知识条目生成按计划第 6.2 节。
- 积分计算按计划第 6.3 节。
- 异常案例生成按计划第 6.4 节。
- 知识图谱组装按计划第 6.5 节。
- seed 数据按计划第 9 节，目标 50-80 条 uploaded_files、45-70 条 knowledge_items、15-20 条 abnormal_cases、50-80 条 contribution_scores、10-15 条 agent_messages。

六、验收命令
在 backend/ 下执行：
python -m venv venv
venv\Scripts\activate
python -m pip install -r requirements.txt
uvicorn main:app --reload --host 0.0.0.0 --port 8000

验收点：
- http://localhost:8000/docs 能看到所有接口。
- employee/123456 和 admin/123456 能登录。
- GET /api/user/profile 带 Bearer token 能返回当前用户。
- POST /api/upload 上传异常图片后，文件保存到 backend/uploads/，数据库新增 uploaded_files、knowledge_items、contribution_scores、abnormal_cases。
- dashboard summary、recent-uploads、ranking、graph 返回数据随上传变化。
- 未配置 AGENT_API_KEY 时 POST /api/agent/chat 返回 mode=mock 或 mock_fallback，不报错。

七、交接输出
完成后请返回：
- 修改/创建的后端文件清单。
- 启动命令和实际启动结果。
- 服务端口：默认 http://localhost:8000。
- 环境变量：列出必须项、可选项、默认值。
- 已验证的 API 清单。
- seed 账号：列出 username/password/role。
- 上传文件访问方式：说明 file_url 如何访问。
- 与本文第 5 节 API 契约的偏差：没有偏差就写“无”。
- 需要前端 Agent B 配合确认的字段或行为。
- 已知问题和未完成项。
```

### 15.3 给 Agent B（前端开发）的提示词

```text
你负责开发“工业现场多模态数据采集与智能管理平台 Demo”的前端部分。

请先阅读全局计划文件：
D:\Deep_learning_project\Demo\工业现场多模态智能管理平台_详细设计计划.md

重点阅读第 0、3、5、6、7、9、11、12、13、15 节。你的工作必须服从全局计划，并与后端 Agent A 并行开发。

一、全局目标
- 首版 MVP 只做本地 Demo：React + Vite + TypeScript 前端，对接 FastAPI 后端。
- 完成员工端：登录、首页、上传、AI 助手、我的贡献。
- 完成管理端：dashboard 大屏、知识库、知识图谱、异常案例、贡献排行榜、Agent 决策。
- 后端未完成时，使用 mock/fixture 数据让页面可独立演示；后端完成后通过 VITE_API_BASE_URL 切真实 API。

二、严格文件边界
- 你只允许创建和修改 frontend/ 目录内文件。
- 不要修改 backend/。
- 不要修改 README.md、根 .gitignore、deploy/、worker/。
- 不要创建 Cloudflare Pages、GitHub Pages、Nginx/systemd 相关实现。

三、建议目录
frontend/
├── package.json
├── vite.config.ts
├── index.html
├── .env.example
├── .env.development
├── src/
│   ├── main.tsx
│   ├── App.tsx
│   ├── router/
│   ├── api/
│   ├── store/
│   ├── layouts/
│   ├── pages/
│   │   ├── Login.tsx
│   │   ├── employee/
│   │   └── admin/
│   ├── components/
│   ├── types/
│   ├── mocks/
│   └── styles/

四、API 契约
- 严格按计划第 5 节定义 TypeScript 类型和 API 方法。
- 所有接口统一从 VITE_API_BASE_URL 读取后端地址，默认 http://localhost:8000。
- 登录成功后保存 token 和 user，后续请求带 Authorization: Bearer <token>。
- 上传页使用 multipart/form-data，字段名与第 5.3 节完全一致。
- mock/fixture 数据结构必须与真实 API 响应保持一致，方便切换。

五、页面要求
- 登录页：统一入口，employee 登录跳 /employee/dashboard，admin 登录跳 /admin/dashboard。
- 员工端：马卡龙浅色风格，低压迫感，操作入口清晰。
- 管理端：蓝紫科技大屏风格，指标卡、实时流、趋势图、排行、异常列表、知识图谱都要适合投屏演示。
- 知识图谱使用 ECharts Graph，节点类型和边关系按第 6.5 节。
- Agent 页面要支持 role_type 切换、问题输入、答案展示、sources/suggestions 或结构化建议展示。
- 全部页面要有 loading、空状态、错误提示；上传要有进度和成功积分反馈。

六、验收命令
在 frontend/ 下执行：
npm install
npm run dev

验收点：
- http://localhost:5173/login 可访问。
- employee/123456 登录进入 /employee/dashboard。
- admin/123456 登录进入 /admin/dashboard。
- 无后端时页面不空白，可用 mock 数据演示主要页面。
- 后端可用时，VITE_API_BASE_URL=http://localhost:8000 后可真实登录、上传、刷新 dashboard、查看图谱、调用 Agent chat。
- 管理端页面无明显布局重叠、文字溢出或控制台阻塞性错误。

七、交接输出
完成后请返回：
- 修改/创建的前端文件清单。
- 启动命令和实际启动结果。
- 服务端口：默认 http://localhost:5173。
- 环境变量：列出 `VITE_API_BASE_URL`、mock/真实 API 切换方式。
- 已完成页面清单。
- 使用的后端接口清单。
- 与本文第 5 节 API 契约的偏差：没有偏差就写“无”。
- 需要后端 Agent A 配合确认的 API 字段或行为。
- 已知问题和未完成项。
```

---

## 最终交付物结构

### 首版 MVP 必交付结构

```text
industrial-multimodal-demo/       # 项目根目录
├── README.md                     # 集成阶段补齐：本地启动/配置/账号/演示说明
├── .gitignore                    # 集成阶段补齐：排除 .env、uploads、db、node_modules 等
├── frontend/                     # React 前端
│   ├── package.json
│   ├── vite.config.ts
│   ├── .env.example
│   ├── .env.development
│   └── src/
│       ├── layouts/EmployeeLayout.tsx
│       ├── layouts/AdminLayout.tsx
│       ├── pages/...
│       └── ...
└── backend/                      # FastAPI 后端（首版 MVP）
    ├── requirements.txt
    ├── main.py
    ├── .env.example
    ├── app/
    └── uploads/
```

### 二期/备选结构（首版不创建）

```text
industrial-multimodal-demo/
├── worker/                       # Cloudflare Workers（方案B）
│   ├── package.json
│   ├── wrangler.toml
│   ├── migrations/
│   └── src/
└── deploy/                       # 部署配置
    ├── nginx.conf
    ├── industrial-demo.service
    └── deploy.sh
```

首版 Agent A/B 不得创建本小节中的 `worker/` 或 `deploy/`，除非后续收到新的明确指令。

---

*本文档基于《工业现场多模态数据采集与智能管理平台 Demo 设计文档 v1.1》整理，作为首版开发实施与双 Agent 并行协作的总纲。*
